// ---------------------------------------------------------------------------
// Aley platform config.
// ---------------------------------------------------------------------------

// The dedicated non-profit "Improve Aley" community platform (its own website).
// Leave this EMPTY to show the built-in "coming soon" placeholder page.
// When the new site is live, just paste its URL here — the "Community" nav item,
// the footer link, and the /community page will all automatically point/redirect
// there. Nothing else needs to change.
export const COMMUNITY_URL = "";
